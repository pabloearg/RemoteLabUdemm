#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createTEACHERInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createTEACHERInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO TEACHER $colStr VALUES $valStr", "SELECT * FROM TEACHER WHERE id=$ctx.args.createTEACHERInput.id"]
}