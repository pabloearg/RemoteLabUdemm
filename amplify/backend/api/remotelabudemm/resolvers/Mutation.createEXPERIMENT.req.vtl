#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createEXPERIMENTInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createEXPERIMENTInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO EXPERIMENT $colStr VALUES $valStr", "SELECT * FROM EXPERIMENT WHERE id=$ctx.args.createEXPERIMENTInput.id"]
}