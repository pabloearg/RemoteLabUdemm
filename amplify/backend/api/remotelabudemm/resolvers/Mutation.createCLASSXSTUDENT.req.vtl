#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCLASSXSTUDENTInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCLASSXSTUDENTInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO CLASSXSTUDENT $colStr VALUES $valStr", "SELECT * FROM CLASSXSTUDENT WHERE id=$ctx.args.createCLASSXSTUDENTInput.id"]
}