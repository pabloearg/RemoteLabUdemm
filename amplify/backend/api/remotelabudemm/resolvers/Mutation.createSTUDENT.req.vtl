#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSTUDENTInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSTUDENTInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO STUDENT $colStr VALUES $valStr", "SELECT * FROM STUDENT WHERE id=$ctx.args.createSTUDENTInput.id"]
}